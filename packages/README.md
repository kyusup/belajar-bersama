# packages/shared

Shared TypeScript contracts for the web app (API DTOs, permission names).

The backend remains the source of truth. These types are for client convenience and must not be used as authorization logic.
