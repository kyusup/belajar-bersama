# Docker image definitions

Phase 2 runs Next.js and Quarkus on the host. Compose at the repository root starts Postgres and MinIO only.

Application Dockerfiles will be added when a deployment environment exists. Do not assume production images yet.
