# infra/

Local Compose lives at the **repository root** (`docker-compose.yml`) so `docker compose up -d` works after clone.

This folder holds supporting notes:

- `docker/` — future application images
- `postgres/` — database notes
